apiVersion: v1
kind: Pod
metadata: {name: sluice-kx, namespace: storage, labels: {owner: kfir-toledo, exp: device-gap-kernel}}
spec:
  restartPolicy: Never
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: kubernetes.io/hostname
            operator: NotIn
            values: ["pokprod-b93r44s0"]
  containers:
  - name: vllm
    image: vllm/vllm-openai:v0.25.1
    command: ["sleep", "infinity"]
    resources:
      requests: {cpu: "16", memory: 360Gi, nvidia.com/gpu: "4"}
      limits:   {cpu: "48", memory: 420Gi, nvidia.com/gpu: "4"}
    env: [{name: HF_HOME, value: /vllm-cache/hf}]
    volumeMounts:
    - {name: vllm-cache, mountPath: /vllm-cache}
    - {name: shm, mountPath: /dev/shm}
    - {name: work, mountPath: /work}
  volumes:
  - {name: vllm-cache, persistentVolumeClaim: {claimName: vllm-cache}}
  - {name: shm, emptyDir: {medium: Memory, sizeLimit: 48Gi}}
  - {name: work, emptyDir: {}}
