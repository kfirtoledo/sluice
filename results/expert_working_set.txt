DeepSeek-V2-Lite (64 routed experts, top_k=6) — measured per-step working set
(SLUICE_DIAG, vLLM 0.23.0, 1xH100, short prompts).

DECODE distinct experts per step, by batch (= the min SLUICE_SLOTS to be lossless):
    batch=1 ->  6   (= top_k)
    batch=2 -> 12
    batch=4 -> 24
    batch=8 -> 40
  (top-6 per token, unioned over the batch -> ~linear growth)

PREFILL chunk (whole chunk in one step) -> 53-61 distinct -> sets the real slot
floor for bit-exact serving on one unsharded GPU.

THROUGHPUT retained vs resident baseline (decode tok/s, valid/bit-identical points):
  slots:        16     32     48     64
  batch=1:     51%    64%    77%    85%
  batch=2:     36%    49%    66%    88%
  batch=4:      -     31%    51%    87%
  batch=8:      -      -     33%    84%
  (baseline tok/s: b1=27, b2=55, b4=108, b8=222)
  At full residency (slots=64) every batch ~85% (routing-hook floor); below it,
  bigger batch = bigger working set = more streaming = more loss.
